# Base URL of the arnav-ai chat API. Unset = the production hostname.
VITE_CHAT_API_BASE=https://chat.arnavjagetia.com

# "true" only on the tailnet-served dev replica (disables analytics, enables
# dev tooling). Never set for a production build.
VITE_DEV_MODE=false
